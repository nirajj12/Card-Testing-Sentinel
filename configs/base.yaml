project_name: Card-Testing Sentinel
random_seed: 42
paths:
  frozen_data: data/frozen
  runtime_data: data/runtime
  artifacts: artifacts
  logs: logs
  reports: reports
  mlflow: mlruns
logging:
  level: INFO
  filename: sentinel.log
  console_enabled: true
frozen_dataset:
  version: v4
  archive_path: razorpay (1).zip
  manifest_filename: manifest.json
  raw_events_filename: raw_events.csv
  enriched_events_filename: events_with_features.csv
  device_splits_filename: device_splits.csv
  report_path: reports/data_validation_report.json
